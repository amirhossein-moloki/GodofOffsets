# راهنمای جامع نصب، کامپایل و استفاده از Universal Offset Dumper 🛡️

این راهنما به شما کمک می‌کند تا پروژه را از مرحله نصب پیش‌نیازها تا استفاده حرفه‌ای از تمامی قابلیت‌های آن، از جمله بخش Kernel Driver، به طور کامل یاد بگیرید.

---

## 📋 فهرست مطالب
1. [پیش‌نیازهای سیستم](#۱-پیش‌نیازهای-سیستم)
2. [کامپایل برنامه اصلی (User-mode)](#۲-کامپایل-برنامه-اصلی-user-mode)
3. [کامپایل و راه‌اندازی کرنل درایور (Kernel Driver)](#۳-کامپایل-و-راه‌اندازی-کرنل-درایور-kernel-driver)
4. [راهنمای استفاده از تب‌های برنامه](#۴-راهنمای-استفاده-از-تب‌های-برنامه)
5. [مثال عملی: پیدا کردن مقدار سلامتی (Health) در یک بازی](#۵-مثال-عملی-پیدا-کردن-مقدار-سلامتی-health-در-یک-بازی)

---

## ۱. پیش‌نیازهای سیستم

قبل از شروع، مطمئن شوید ابزارهای زیر روی ویندوز شما نصب هستند:

### برای برنامه اصلی (UI):
- **Visual Studio 2022:** هنگام نصب، گزینه "Desktop development with C++" را تیک بزنید.
- **CMake:** نسخه 3.20 یا بالاتر (قابل دانلود از [cmake.org](https://cmake.org/download/)).
- **Git:** برای دریافت کد منبع.

### برای بخش کرنل درایور (Stealth Mode):
- **Windows Driver Kit (WDK):** باید متناسب با نسخه ویندوز و ویژوال استودیو شما نصب شود.
- **SDK ویندوز:** معمولاً همراه با ویژوال استودیو نصب می‌شود.

---

## ۲. کامپایل برنامه اصلی (User-mode)

### روش پیشنهادی (استفاده از Visual Studio):
1. پوشه پروژه را در Visual Studio باز کنید (Open a local folder).
2. ویژوال استودیو فایل `CMakeLists.txt` را شناسایی کرده و کش CMake را می‌سازد.
3. در منوی بالا، پیکربندی را روی **x64-Release** قرار دهید.
4. از منوی **Build**، گزینه **Build All** را بزنید.
5. پس از اتمام، فایل `UniversalOffsetDumper.exe` در مسیر `out/build/x64-Release/` ساخته می‌شود.

### روش خط فرمان (Command Line):
```bash
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . --config Release
```

---

## ۳. کامپایل و راه‌اندازی کرنل درایور (Kernel Driver)

بخش Stealth Mode برای دور زدن آنتی‌چیت‌ها از یک درایور سطح هسته (Ring 0) استفاده می‌کند.

### مراحل کامپایل درایور:
1. فایل `KernelDumper.sln` (یا در صورت عدم وجود، ایجاد یک پروژه `KMDF Driver` جدید و افزودن فایل‌های `src/Kernel/Driver.c` و `src/Kernel/Driver.h`) را در Visual Studio باز کنید.
2. پلتفرم را روی **x64** و پیکربندی را روی **Release** قرار دهید.
3. پروژه را Build کنید تا فایل `KernelDumper.sys` ایجاد شود.

### مراحل استفاده (مهم):
1. فایل `KernelDumper.sys` را در کنار فایل اصلی برنامه (`UniversalOffsetDumper.exe`) قرار دهید.
2. ویندوز را در حالت **Test Mode** قرار دهید (چون درایور امضای دیجیتال رسمی ندارد):
   - ترمینال را در حالت Administrator باز کنید و بزنید: `bcdedit /set testsigning on`
   - سیستم را ریستارت کنید.
3. برنامه را با دسترسی **Administrator** اجرا کنید تا بتواند درایور را لود کند.

---

## ۴. راهنمای استفاده از تب‌های برنامه

### [P] تب Process (اتصال به برنامه)
- **لیست پروسس‌ها:** بازی یا برنامه هدف را انتخاب کنید.
- **Standard Mode:** استفاده از توابع معمولی ویندوز (برای برنامه‌های عادی).
- **Stealth Mode:** استفاده از کرنل درایور (برای بازی‌های دارای آنتی‌چیت).
- در پایین این تب، ماژول‌های لود شده و بخش‌های (Sections) حافظه نمایش داده می‌شوند.

### [M] تب Memory Scanner (جستجوی مقادیر)
1. **Data Type:** نوع داده (مثلاً Float، Int32 یا String16 برای رشته‌های یونیکد) را انتخاب کنید.
2. **First Scan:** مقدار فعلی را وارد کرده و جستجو کنید.
3. **Next Scan:** پس از تغییر مقدار در بازی، مقدار جدید را وارد کرده و فیلتر کنید.
- **Undo/Reset:** برای بازگشت به مرحله قبل یا شروع اسکن جدید.

### [S] تب Signature Scanner (استخراج آفست‌ها)
- این بخش از فایل `signatures.json` استفاده می‌کند.
- با زدن **Run Signatures Scan**، برنامه به دنبال الگوهای بایت (Patterns) می‌گردد.
- **Export:** می‌توانید نتایج را به صورت فایل `.h` (برای C++)، `.cs` (برای #C) یا `.rs` (برای Rust) خروجی بگیرید.

### [T] تب Pointer Scan (پیدا کردن اشاره‌گرهای ثابت)
- اگر آدرسی را پیدا کردید که با هر بار بستن و باز کردن بازی تغییر می‌کند، از این تب استفاده کنید.
- آدرس فعلی را بدهید تا برنامه "زنجیره اشاره‌گر" (Pointer Chain) ثابت از ماژول اصلی را پیدا کند.

### [H] تب Hex Viewer (مشاهده مستقیم حافظه)
- نمایش زنده بایت‌های حافظه. بایت‌های صفر با خاکستری، حروف ASCII با سبز و بایت‌های خاص با قرمز نمایش داده می‌شوند.

---

## ۵. مثال عملی: پیدا کردن مقدار سلامتی (Health) در یک بازی

فرض کنید می‌خواهیم در بازی `Tutorial.exe` مقدار سلامتی را پیدا کنیم:

1. **اتصال:** در تب **Process**، برنامه `Tutorial.exe` را پیدا کرده و **Attach** کنید.
2. **اسکن اول:** در تب **Memory Scanner**، نوع داده را روی **Int32** بگذارید. اگر سلامتی شما ۱۰۰ است، عدد ۱۰۰ را وارد کرده و **First Scan** را بزنید. (ممکن است هزاران نتیجه پیدا شود).
3. **تغییر مقدار:** در بازی کاری کنید که سلامتی شما کم شود (مثلاً بشود ۸۵).
4. **اسکن دوم:** عدد ۸۵ را در برنامه وارد کرده و **Next Scan** را بزنید. حالا لیست نتایج بسیار کوتاه می‌شود.
5. **تأیید:** دوباره سلامتی را تغییر دهید و اسکن کنید تا فقط ۱ یا ۲ آدرس باقی بماند.
6. **مشاهده در هگز:** روی آدرس پیدا شده دوبار کلیک کنید تا در تب **Hex Viewer** موقعیت دقیق آن را در حافظه ببینید.
7. **ساخت امضا:** اگر می‌خواهید این آدرس را برای همیشه داشته باشید، می‌توانید یک پترن از بایت‌های اطراف آن بسازید و در `signatures.json` قرار دهید.

---
*این ابزار برای اهداف آموزشی ساخته شده است. لطفاً مسئولانه استفاده کنید.*
